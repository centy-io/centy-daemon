# Auto infer orgs

We want to auto-infer org according to the location of the `.centy` project, i.e. if in our example the folder is inside a repo centy-cli and the repository is part of the organization `centy` we can infer that the project belong to the `centy` org
