# Config file

When updating the `config.json` file we want to update the json file only with the changed options, so we dont want to have the default options as well, and when getting a patial configuration we will just merge it with the default options.
