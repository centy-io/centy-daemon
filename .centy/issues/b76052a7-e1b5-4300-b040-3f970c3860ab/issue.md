# Agents use

We want to let the user send a ststic message that will always come in the context when an agent running the cli.

we need to brainstorm this idea.

For example when the user saying to the llm to always read all the docs first
