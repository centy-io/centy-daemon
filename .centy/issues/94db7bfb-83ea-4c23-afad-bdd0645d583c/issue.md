# Daemon icon

We want to have the daemon icon showen in the windows right side running services and the macos top right background services as well as linux operation systems, from this icon they can restart or turn off the daemon as well as if they have the desktop app open the desktop app or if they have the `centy` cli installed in global PATH open a terminal with the tui open
