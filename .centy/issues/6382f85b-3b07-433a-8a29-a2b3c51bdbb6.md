---
# This file is managed by Centy. Use the Centy CLI to modify it.
displayNumber: 364
status: open
priority: 2
createdAt: 2026-03-26T18:38:46.834682+00:00
updatedAt: 2026-03-26T19:14:46.433514+00:00
---

# Migrate link storage to per-link markdown files with frontmatter

## Summary

Change how links are stored: instead of per-entity JSON files (`.centy/{type}s/links/{entity_id}/links.json`), each link becomes its own markdown file at `.centy/links/{uuid}.md` with all data in YAML frontmatter — making links consistent with how generic items are stored.

## New storage format

```
.centy/links/{uuid}.md
```

Frontmatter fields:
- `sourceId` — source entity UUID
- `sourceType` — singular item type (e.g. `issue`)
- `targetId` — target entity UUID
- `targetType` — singular item type
- `linkType` — relationship type (e.g. `blocks`)
- `createdAt` / `updatedAt` — timestamps from mdstore

The markdown body is intentionally empty by default — users may write whatever freeform content they like there (notes, context, etc.). The daemon ignores it.

## Key decisions

- **One file per relationship** (no separate inverse file; inverse synthesized at query time)
- **No display numbers**
- **Drop old format** (no backward compat migration)
- Use `mdstore::create/delete/list` with `TypeFeatures::default()` (all features off)
- **Delete by link UUID** — `delete_link` takes the link file UUID directly; no scanning by source/target

## Changes required

- New `LinkRecord` struct (full both-sides record) + `source_link()` / `target_link(inverse)` view methods
- Rewrite `storage/io.rs` to use mdstore on `.centy/links/`
- Delete `storage/links_file.rs` and `LinksFile` type
- Rewrite `crud_fns/create.rs` — one file, synthesize both response views
- Rewrite `crud_fns/delete.rs` — delete by link UUID (mdstore delete by id)
- Rewrite `crud_read.rs` — `list_links` scans all files, filters by entity, synthesizes inverse for target matches; add `custom_types: &[CustomLinkTypeDefinition]` param
- Update `link/mod.rs` exports (remove `LinksFile`, `read_links`, `write_links`)
- Update `server/handlers/link_read.rs` — fetch `custom_types` before calling `list_links`
- Rewrite `links_file_*` tests for new storage model

## Why `list_links` needs `custom_types`

With one file per relationship, scanning links for entity B finds two kinds of matches:

- `sourceId = B` — `linkType` is already B's perspective, no transformation needed
- `targetId = B` — the file stores the forward direction (e.g. `linkType: "blocks"`), but B's view requires the inverse (e.g. `"blocked-by"`)

For built-in types, `get_inverse_link_type` handles the lookup. For custom types, the inverse string only exists in `CustomLinkTypeDefinition.inverse` — there's no other way to derive it. So `custom_types` is required to synthesize the correct `linkType` for all target-side matches.
