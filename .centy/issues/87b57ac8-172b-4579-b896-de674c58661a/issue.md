# API versioning

We want to have the option to update the cli and the client and to maintain a backwarde compatibilty with the daemon and vice versa, in the daemon we work with grpc so we want to have versioning to the api so when publising a new cli for example it will still be able to work with old daemon and vice versa, do not implement this task, this is need to be planed toghther
