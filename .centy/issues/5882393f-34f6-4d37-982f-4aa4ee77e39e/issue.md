# Configurable IDE

We want to let the user configure the IDE that the workspace will be open with, at the moment it's only VSCode, but we want let him set the IDE in the user scope config and also in a project-specific config. Please open issues for the CLI and TUI as well
